package com.fsm.task.infrastructure.security;

/**
 * Role enum representing user roles in the FSM system.
 * Matches the roles defined in the identity-svc.
 */
public enum Role {
    ADMIN,
    DISPATCHER,
    SUPERVISOR,
    TECHNICIAN
}
